<Page x:Class="ЖКХ.AuthorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ЖКХ"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация"
      Background="#FFE6E6E6">

    <Grid>
        <Border Style="{StaticResource AuthBorderStyle}" Width="300">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Система ЖКХ" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Логин:"/>
                <TextBox x:Name="loginTextBox"/>
                <TextBlock Text="Пароль:"/>
                <PasswordBox x:Name="passwordText"/>
                <Button Content="Войти" Click="loginButton_Click" 
                        Style="{StaticResource AuthButtonStyle}" Width="100"/>
                <Button Content="Регистрация" Click="registerButton_Click" 
                        Style="{StaticResource AuthButtonStyle}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>